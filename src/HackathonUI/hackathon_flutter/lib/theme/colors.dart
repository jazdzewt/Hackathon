import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color.fromARGB(255, 13, 24, 38);
  static const Color text = Color.fromARGB(255, 13, 24, 38);
  static const Color background = Color(0xFFF5F7FA);
  static const Color accent = Color(0xFFFFC107);
  static const Color shadows = Color.fromARGB(70, 0, 0, 0);
}
